schema_version: "vyges-upstream-v1"
upstream:
  type: "monorepo"
  source: https://github.com/lowRISC/opentitan
  path: hw/ip/rv_core_ibex
  commit: 0ec7cc703be746941ee69a9a723b6ea08c66b3d9
  extracted_at: "2026-03-23T02:09:25Z"
import:
  rtl:
    - path: "rtl"
      map_to: "rtl"
  license:
    - path: "apache-2.0"
  readme:
    - path: "README.md"
license:
  spdx: Apache-2.0
  file: apache-2.0
metadata_locked: true
# Vyges-authored content — excluded from upstream sync. See vyges-metadata.json
# vyges_additions[] for the list.
vyges_authored:
  exclude_from_sync:
    - "vyges-additions/"
# Sync policy for future vyges-overlays/ (when present): upstream pulls proceed;
# any upstream change to a path listed in vyges_overlays[].replaces is flagged
# for human review.
sync_policy:
  overlay_conflict: "flag-for-review"
